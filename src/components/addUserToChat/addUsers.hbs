<div class="{{styles.modal__box}}">
    <div class="{{styles.modal__container}}">
        {{{input}}}
        {{{button}}}
        <div class="{{styles.usersInChat}}">
            {{{usersInChat}}}
            {{{deleteButton}}}
        </div>
        {{{users}}}
    </div>
</div>

