{{#each profileData}}
    <div class="{{../profileBoxStyle}}">
            <h class="{{../headerStyle}}">{{label}}</h>
            <input type="password" class="{{../headerInfo}}" name="{{name}}" placeholder="{{value}}">
    </div>
{{/each}}